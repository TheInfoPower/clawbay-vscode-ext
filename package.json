{
  "name": "clawbay-quota",
  "displayName": "Clawbay Quota",
  "description": "Track your Clawbay API spending without leaving VS Code. Shows 5-hour and weekly usage in the status bar — hover for cost breakdown and time until reset.",
  "version": "0.0.5",
  "publisher": "theInfoPower",
  "license": "MIT",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/TheInfoPower/clawbay-vscode-ext.git"
  },
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "clawbayQuota.refresh",
        "title": "Clawbay Quota: Refresh"
      },
      {
        "command": "clawbayQuota.retry",
        "title": "Clawbay Quota: Retry"
      },
      {
        "command": "clawbayQuota.setToken",
        "title": "Clawbay Quota: Set Token"
      },
      {
        "command": "clawbayQuota.clearToken",
        "title": "Clawbay Quota: Clear Token"
      },
      {
        "command": "clawbayQuota.authStatus",
        "title": "Clawbay Quota: Show Auth Status"
      },
      {
        "command": "clawbayQuota.login",
        "title": "Clawbay Quota: Login"
      },
      {
        "command": "clawbayQuota.logout",
        "title": "Clawbay Quota: Logout"
      }
    ],
    "configuration": {
      "title": "Clawbay Quota",
      "properties": {
        "clawbayQuota.refreshIntervalMinutes": {
          "type": "number",
          "default": 5,
          "description": "How often to auto-refresh quota data (minutes). Set to 0 to disable."
        },
        "clawbayQuota.statusBarAlignment": {
          "type": "string",
          "enum": [
            "left",
            "right"
          ],
          "default": "left",
          "description": "Where the Clawbay status item appears."
        },
        "clawbayQuota.apiBaseUrl": {
          "type": "string",
          "default": "https://theclawbay.com/api/codex-auth/v1",
          "description": "Base URL for the quota API (the extension appends /quota if needed)."
        },
        "clawbayQuota.apiToken": {
          "type": "string",
          "scope": "application",
          "default": "",
          "description": "Clawbay API token persisted in global user settings so login remains across sessions and repositories."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile",
    "test": "node ./out/test/run-test.js",
    "test:ci": "npm run compile && VSCODE_SKIP_LAUNCH_TESTS=1 node ./out/test/run-test.js",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.17.30",
    "@types/vscode": "^1.95.0",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.7.1",
    "mocha": "^10.8.2",
    "typescript": "^5.8.2",
    "zod": "^3.24.2"
  },
  "overrides": {
    "mocha": {
      "serialize-javascript": "^7.0.3"
    },
    "picomatch": "^2.3.2"
  }
}
